.DS_Store
*.pdf
*.pkl
*.h5
*.html
*.png
!figures/det_repo_img.png
!figures/combined_3x2_charge_displays.png
*.npy
*.root
sftp-config.json

# Generated data from scripts
**/generated_data/
**/spatial_overlap_integrals/
**/__pycache__/
**/.ipynb_checkpoints/
**/test_events/
**/autodiff_datasets/
**/output/
**/trained_model/
**/data/
**/siren_training/**
**/dedx_siren_training/**
**/debug/
!data/
!data/**/
!data/**/photonsim_params.json

# Ignore s3df_jobs folder except .py and .md files
s3df_jobs/*
!s3df_jobs/*.py
!s3df_jobs/*.md
!s3df_jobs/*/
s3df_jobs/*/*
!s3df_jobs/*/*.py
!s3df_jobs/*/*/
s3df_jobs/*/*/*
!s3df_jobs/*/*/*.py





lucid_sim.egg-info/
build/
campaign/grid_out/

# Per-user cluster config (copied from a user_paths.*.sh.template); never commit
lucid/production/jobs/user_paths.sh

# Personal dev/research scratch (kept on disk, not tracked)
hessian_probe/
scripts/campaign_recon/
baseline_scripts/
mie_hunter/

# Executed notebooks (rendered copies) — never commit; regenerate or build in CI
*.executed.ipynb
# Generated animations (e.g. tutorials/event.gif from animate_event)
*.gif

# MkDocs build output
site/

# Trained SIREN surrogates under data/ — these are SYMLINKS into the data area, so no
# trailing slash (a dir-only pattern cannot match a symlink), and they must come
# after the `!data/**/` negation above, which would otherwise re-include them.
data/*/*/siren_training
data/*/*/dedx_siren_training

# Kerberos credential caches. On LXPLUS a stray KRB5CCNAME lands a ccache named
# after the user in whatever directory the job ran from; these hold live tickets
# and must never be committed. Not *.cc — that would mask real C++ sources.
cjesus.cc
krb5cc_*

# Core dumps from crashed batch jobs
core.[0-9]*
